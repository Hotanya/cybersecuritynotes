# AWS Setup

* Setup free AWS account - Can use virtual credit card and gmail alias
* Find a domain
* Add domain to Cloudflare for legitimacy and security
*   Go to EC2

    * Launch Instance
    * Select a t2 micro instance - debian etc
    * generate ssh keys
    * 15 gb storage
    * create


* SSH into instance
* `sudo apt-get update`
* `sudo apt-get upgrade`
* `sudo apt -y install golang tmux git make certbot`



Cont in gophish
